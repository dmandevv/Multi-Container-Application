# Multi-Container-Application
The goal of this project is to practice using Docker Compose to run a multi-container application in production. Uses Docker Compose to run a Node.js application and a MongoDB database.
